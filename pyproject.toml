[project]
name = "qwen3-ink-cpp"
version = "0.1.0"
requires-python = "==3.10.15"
dependencies = [
    "accelerate>=1.8.1",
    "datasets>=3.6.0",
    "ipykernel>=6.29.5",
    "lm-eval[vllm]",
    "torch==2.6.0",
    "transformers>=4.52.4",
]

[tool.uv.sources]
lm-eval = { path = "lm-evaluation-harness", editable = true }
